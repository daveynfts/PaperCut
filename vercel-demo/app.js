"use strict";

// The original client-only payment demo was intentionally retired because it
// stored premium content and unlock decisions in the browser. The maintained
// application lives under /papercut/ and verifies access on the API.
window.location.replace("/papercut/");
